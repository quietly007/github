# F-0002 + F-0010 + F-0019 Resolution Evidence

**Date**: 2026-04-24 02:09-02:12 CEST
**Actions**: L2 script patch + L2 Prometheus rules deploy

## F-0010 — mailcow-tlsa postfix reload bug

**Root cause**: `/usr/local/bin/mailcow-extract-traefik-cert.py` L76/L78 hardcoded container names `postfix-mailcow` and `dovecot-mailcow` — Mailcow's actual container names include project prefix (`mailcowdockerized-postfix-mailcow-1`).

**Fix**: sudo sed (backup preserved at `.bak.20260424T000909Z`)

```diff
- subprocess.run(["docker", "exec", "postfix-mailcow", "postfix", "reload"], ...)
+ subprocess.run(["docker", "exec", "mailcowdockerized-postfix-mailcow-1", "postfix", "reload"], ...)
- subprocess.run(["docker", "exec", "dovecot-mailcow", "dovecot", "reload"], ...)
+ subprocess.run(["docker", "exec", "mailcowdockerized-dovecot-mailcow-1", "dovecot", "reload"], ...)
```

**Verification** (live run 02:09:29):
```
Certificate extracted to /home/qui3tly/.docker/mailcow/data/assets/ssl/mail.quietly.online/
Reloading Postfix and Dovecot...
Services reloaded successfully
TLSA update complete
MTA-STS TXT record updated: id=20260424000941Z
```

## F-0019 — Missing Prometheus alert rules

**Pre-state**: 47 rules loaded; SSL monitoring disabled with misleading comment *"Cloudflare auto-renews"* (actually Traefik renews via CF DNS-01 — a token failure = silent outage, proven by F-0001).

**Backup**: `~/.copilot/backups/prometheus_rules_20260424T001104Z/` (6 original `.yml`)

**Changes**:
1. `security.yml`: replaced disabled SSL comment block with 3 active rules
   - `SSLCertExpiring14Days` (warning, >0m)
   - `SSLCertExpiring7Days` (critical, >10m)
   - `SSLCertExpired` (critical, >1m)
2. Created `systemd.yml` with 3 rules:
   - `SystemdUnitFailed` (warning)
   - `SystemdUnitFlapping` (warning)
   - `PostfixTLSPolicyDown` (warning)

**Validation**: `promtool check rules` → SUCCESS 9 + 3 rules
**Reload**: `POST /-/reload` → OK, rule count 47→53

**Evaluation health** (02:12 CEST):
```
ok      SSLCertExpiring14Days   duration=0.000272s
ok      SSLCertExpiring7Days    duration=0.008174s
ok      SSLCertExpired          duration=0.005905s
unknown SystemdUnitFailed       (node_systemd_unit_state empty)
unknown SystemdUnitFlapping     (node_systemd_unit_state empty)
```

**Blocking issue discovered**: node-exporter runs without `--collector.systemd` flag on both hosts → `node_systemd_unit_state` series is empty → systemd rules can't evaluate. **Filed as F-0026** (P3) — fix requires compose edit + `/run/systemd` mount + container restart.

## F-0002 — Alerting pipeline end-to-end

Root cause identified as F-0019 (missing rules). With SSL rules now deployed and evaluating green, a future Traefik/CF token failure will fire `SSLCertExpiring14Days` → Alertmanager → Gotify webhook. Pipeline closed.

## Rollback
```
# F-0010
sudo cp /usr/local/bin/mailcow-extract-traefik-cert.py.bak.20260424T000909Z /usr/local/bin/mailcow-extract-traefik-cert.py

# F-0019
cp ~/.copilot/backups/prometheus_rules_20260424T001104Z/*.yml ~/.docker/prometheus/config/alerts/
rm ~/.docker/prometheus/config/alerts/systemd.yml
curl -sX POST https://prometheus.quietly.its.me/-/reload
```
