# FRESH INSTALL - Brand new Contabo servers
# Use for: Initial access, BEFORE any Ansible runs
# These connect via public IP, port 22, admin user, password auth

[all:vars]
ansible_user=admin
ansible_port=22
ansible_ssh_common_args='-o StrictHostKeyChecking=accept-new -o UserKnownHostsFile=/dev/null'

# ─────────────────────────────────────────────────────────────
# FRESH SERVERS - Public IPs, admin user, port 22
# ─────────────────────────────────────────────────────────────
[servers]
master ansible_host=213.136.68.108
lady ansible_host=207.180.251.111

[master_group]
master

[workers]
lady

[linux:children]
servers
